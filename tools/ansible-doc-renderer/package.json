{
  "name": "@cisco/ansible-doc-renderer",
  "version": "0.1.0",
  "description": "Render ansible-doc JSON into static HTML documentation sites",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "bin": {
    "ansible-doc-render": "./bin/ansible-doc-render"
  },
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "clean": "rm -rf out"
  },
  "files": [
    "out",
    "bin"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
