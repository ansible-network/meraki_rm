---
- name: "Cleanup \u2014 switch_routing (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: switch_routing does not support delete
