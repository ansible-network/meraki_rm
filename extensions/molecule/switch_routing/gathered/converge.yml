---
- name: "Converge \u2014 switch_routing (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current switch_routing configuration
    cisco.meraki_rm.meraki_switch_routing:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
