---
- name: "Prepare \u2014 switch_routing (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_switch_routing:
      network_id: N_switch_routing_check
      state: merged
      config:
      - '{{ prepare_config }}'
