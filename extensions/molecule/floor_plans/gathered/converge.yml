---
- name: "Converge \u2014 floor_plans (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current floor_plans configuration
    cisco.meraki_rm.meraki_floor_plans:
      network_id: N_floor_plans_gathered
      state: gathered
    register: gathered
