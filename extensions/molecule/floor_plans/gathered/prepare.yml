---
- name: "Prepare \u2014 floor_plans (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_floor_plans:
      network_id: N_floor_plans_gathered
      state: merged
      config:
      - name: Test-Config
        width: 0.0
        height: 0.0
        floor_number: 0.0
        image_contents: example
        image_extension: example
