---
- name: "Cleanup \u2014 floor_plans (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_floor_plans:
      network_id: N_floor_plans_gathered
      state: deleted
      config:
      - floor_plan_id: example
    ignore_errors: true
