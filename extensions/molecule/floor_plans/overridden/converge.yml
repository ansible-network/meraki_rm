---
- name: "Converge \u2014 floor_plans (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: "Override all floor_plans \u2014 desired state only"
    cisco.meraki_rm.meraki_floor_plans:
      network_id: N_123456789012345678
      state: overridden
      config:
      - '{{ expected_config }}'
    register: override_result
