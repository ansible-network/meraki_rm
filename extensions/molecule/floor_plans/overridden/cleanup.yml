---
- name: "Cleanup \u2014 floor_plans (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_floor_plans:
      network_id: N_floor_plans_overridden
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
