---
- name: "Prepare — floor_plans (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for overridden)
      cisco.meraki_rm.meraki_floor_plans:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
