---
- name: "Prepare — appliance_static_routes (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for overridden)
      cisco.meraki_rm.meraki_appliance_static_routes:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
