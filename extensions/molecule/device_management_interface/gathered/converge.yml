---
- name: "Converge \u2014 device_management_interface (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current device_management_interface configuration
    cisco.meraki_rm.meraki_device_management_interface:
      serial: Q2XX-device_management_interface-gathered
      state: gathered
    register: gathered
