---
- name: "Prepare — device_management_interface (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_device_management_interface:
        serial: Q2XX-device_management_interface-gathered
        state: merged
        config:
          - "{{ expected_config }}"
