---
- name: "Prepare \u2014 device_management_interface (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_device_management_interface:
      serial: Q2XX-device_management_interface-check
      state: merged
      config:
      - '{{ prepare_config }}'
