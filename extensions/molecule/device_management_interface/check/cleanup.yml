---
- name: "Cleanup \u2014 device_management_interface (check)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup needed for singleton resource
    ansible.builtin.debug:
      msg: device_management_interface does not support delete
