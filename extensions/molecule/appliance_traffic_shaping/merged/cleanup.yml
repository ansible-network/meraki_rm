---
- name: "Cleanup \u2014 appliance_traffic_shaping (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: appliance_traffic_shaping does not support delete
