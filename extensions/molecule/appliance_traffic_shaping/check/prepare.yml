---
- name: "Prepare \u2014 appliance_traffic_shaping (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_appliance_traffic_shaping:
      network_id: N_appliance_traffic_shaping_check
      state: merged
      config:
      - '{{ prepare_config }}'
