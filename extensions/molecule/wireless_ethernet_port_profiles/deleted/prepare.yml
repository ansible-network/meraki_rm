---
- name: "Prepare \u2014 wireless_ethernet_port_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_wireless_ethernet_port_profiles:
      network_id: N_wireless_ethernet_port_profiles_deleted
      state: merged
      config:
      - profile_id: example
        name: Test-Config
        is_default: true
