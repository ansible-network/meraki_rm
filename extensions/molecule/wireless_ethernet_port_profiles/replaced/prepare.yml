---
- name: "Prepare \u2014 wireless_ethernet_port_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_wireless_ethernet_port_profiles:
      network_id: N_wireless_ethernet_port_profiles_replaced
      state: merged
      config:
      - profile_id: example
        name: Test-Config
        is_default: true
