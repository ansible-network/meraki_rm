---
- name: "Prepare \u2014 wireless_ethernet_port_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_wireless_ethernet_port_profiles:
      network_id: N_wireless_ethernet_port_profiles_overridden
      state: merged
      config:
      - name: Test-Config
      - name: Extra-To-Delete
