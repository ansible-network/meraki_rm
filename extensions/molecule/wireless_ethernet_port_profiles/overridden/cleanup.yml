---
- name: "Cleanup \u2014 wireless_ethernet_port_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_wireless_ethernet_port_profiles:
      network_id: N_wireless_ethernet_port_profiles_overridden
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
