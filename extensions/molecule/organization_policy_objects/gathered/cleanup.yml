---
- name: "Cleanup \u2014 organization_policy_objects (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_policy_objects:
      organization_id: org_organization_policy_objects_gathered
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
