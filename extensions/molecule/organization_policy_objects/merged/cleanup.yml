---
- name: "Cleanup \u2014 organization_policy_objects (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_policy_objects:
      organization_id: org_organization_policy_objects_merged
      state: deleted
      config:
      - policy_object_id: example
    ignore_errors: true
