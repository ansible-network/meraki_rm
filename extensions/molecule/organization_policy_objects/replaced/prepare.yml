---
- name: "Prepare — organization_policy_objects (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_organization_policy_objects:
        organization_id: '123456'
        state: merged
        config:
          - "{{ expected_config }}"
