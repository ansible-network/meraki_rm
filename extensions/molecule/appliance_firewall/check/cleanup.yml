---
- name: "Cleanup \u2014 appliance_firewall (check)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup needed for singleton resource
    ansible.builtin.debug:
      msg: appliance_firewall does not support delete
