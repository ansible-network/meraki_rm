---
- name: "Prepare \u2014 appliance_firewall (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_appliance_firewall:
      network_id: N_appliance_firewall_check
      state: merged
      config:
      - '{{ prepare_config }}'
