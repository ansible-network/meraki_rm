---
- name: "Prepare \u2014 appliance_firewall (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_appliance_firewall:
      network_id: N_appliance_firewall_replaced
      state: merged
      config:
      - syslog_default_rule: true
        access: example
        service: example
