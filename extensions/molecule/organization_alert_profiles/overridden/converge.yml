---
- name: "Converge — organization_alert_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current organization_alert_profiles to get server-assigned IDs
    cisco.meraki_rm.meraki_organization_alert_profiles:
      organization_id: '123456'
      state: gathered
    register: current

  - name: Build config with actual server-assigned PK
    ansible.builtin.set_fact:
      overridden_config: >-
        {{ expected_config | combine({'alert_config_id': current.gathered[0].alert_config_id}) }}
    when: current.gathered | length > 0

  - name: Override organization_alert_profiles configuration
    cisco.meraki_rm.meraki_organization_alert_profiles:
      organization_id: '123456'
      state: overridden
      config:
      - "{{ overridden_config }}"
    register: overridden_result
    when: overridden_config is defined
