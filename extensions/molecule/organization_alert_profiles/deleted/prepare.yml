---
- name: "Prepare \u2014 organization_alert_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_organization_alert_profiles:
      organization_id: org_organization_alert_profiles_deleted
      state: merged
      config:
      - type: appOutage
        enabled: true
        description: Managed by Ansible
