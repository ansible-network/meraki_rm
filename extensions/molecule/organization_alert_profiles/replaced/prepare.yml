---
- name: "Prepare — organization_alert_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_organization_alert_profiles:
        organization_id: 'org_organization_alert_profiles_replaced'
        state: merged
        config:
          - "{{ expected_config }}"
