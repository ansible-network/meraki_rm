---
- name: "Cleanup \u2014 organization_alert_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_alert_profiles:
      organization_id: org_organization_alert_profiles_gathered
      state: deleted
      config:
      - alert_config_id: example
    ignore_errors: true
