---
- name: "Prepare \u2014 organization_alert_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_organization_alert_profiles:
      organization_id: org_organization_alert_profiles_gathered
      state: merged
      config:
      - type: access
        enabled: true
        description: Managed by Ansible
