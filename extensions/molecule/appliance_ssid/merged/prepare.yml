---
- name: "Prepare — appliance_ssid (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reset SSID 1 to baseline before test
      cisco.meraki_rm.meraki_appliance_ssid:
        network_id: N_123456789012345678
        state: replaced
        config:
          - number: 1
            name: "Unconfigured SSID 1"
            enabled: false
            auth_mode: open
            encryption_mode: wpa
            psk: ""
            visible: false
