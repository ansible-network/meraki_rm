---
- name: "Cleanup — appliance_ssid (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reset SSID 2 to baseline after test
      cisco.meraki_rm.meraki_appliance_ssid:
        network_id: N_123456789012345678
        state: replaced
        config:
          - number: 2
            name: "Unconfigured SSID 2"
            enabled: false
            auth_mode: open
            encryption_mode: wpa
            psk: ""
            visible: false
