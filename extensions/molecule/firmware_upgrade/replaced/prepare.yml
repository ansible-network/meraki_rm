---
- name: "Prepare — firmware_upgrade (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_firmware_upgrade:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
