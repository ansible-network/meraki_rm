---
- name: "Cleanup \u2014 firmware_upgrade (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: firmware_upgrade does not support delete
