---
- name: "Prepare — firmware_upgrade (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_firmware_upgrade:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
