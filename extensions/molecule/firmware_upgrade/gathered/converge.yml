---
- name: "Converge \u2014 firmware_upgrade (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current firmware_upgrade configuration
    cisco.meraki_rm.meraki_firmware_upgrade:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
