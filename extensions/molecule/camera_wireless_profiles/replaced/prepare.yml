---
- name: "Prepare \u2014 camera_wireless_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_camera_wireless_profiles:
      network_id: N_123456789012345678
      state: merged
      config:
      - wireless_profile_id: example
        name: Test-Config
