---
- name: "Converge \u2014 camera_wireless_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete camera_wireless_profiles configuration
    cisco.meraki_rm.meraki_camera_wireless_profiles:
      network_id: N_camera_wireless_profiles_deleted
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
