---
- name: "Prepare \u2014 camera_wireless_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_camera_wireless_profiles:
      network_id: N_camera_wireless_profiles_overridden
      state: merged
      config:
      - name: Test-Config
      - name: Extra-To-Delete
