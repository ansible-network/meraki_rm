---
- name: "Cleanup \u2014 camera_wireless_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_camera_wireless_profiles:
      network_id: N_camera_wireless_profiles_overridden
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
