---
- name: "Prepare \u2014 camera_wireless_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_camera_wireless_profiles:
      network_id: N_camera_wireless_profiles_gathered
      state: merged
      config:
      - name: Test-Config
