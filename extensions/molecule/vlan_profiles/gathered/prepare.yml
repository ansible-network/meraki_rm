---
- name: "Prepare \u2014 vlan_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_vlan_profiles:
      network_id: N_123456789012345678
      state: merged
      config:
      - iname: example
        name: Test-Config
        is_default: true
