---
- name: "Cleanup \u2014 vlan_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_vlan_profiles:
      network_id: N_vlan_profiles_overridden
      state: deleted
      config:
      - iname: example
    ignore_errors: true
