---
- name: "Converge \u2014 vlan_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Replace vlan_profiles configuration
    cisco.meraki_rm.meraki_vlan_profiles:
      network_id: N_123456789012345678
      state: replaced
      config:
      - '{{ expected_config }}'
    register: replace_result
