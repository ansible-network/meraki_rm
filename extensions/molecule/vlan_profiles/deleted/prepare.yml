---
- name: "Prepare \u2014 vlan_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_vlan_profiles:
      network_id: N_vlan_profiles_deleted
      state: merged
      config:
      - iname: example
        name: Test-Config
        is_default: true
