---
- name: "Cleanup \u2014 appliance_warm_spare (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: appliance_warm_spare does not support delete
