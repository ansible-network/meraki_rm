---
- name: "Converge \u2014 appliance_warm_spare (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current appliance_warm_spare configuration
    cisco.meraki_rm.meraki_appliance_warm_spare:
      network_id: N_appliance_warm_spare_gathered
      state: gathered
    register: gathered
