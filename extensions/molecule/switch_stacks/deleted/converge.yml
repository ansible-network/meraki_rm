---
- name: "Converge \u2014 switch_stacks (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete switch_stacks configuration
    cisco.meraki_rm.meraki_switch_stacks:
      network_id: N_123456789012345678
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
