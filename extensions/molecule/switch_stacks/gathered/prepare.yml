---
- name: "Prepare \u2014 switch_stacks (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_switch_stacks:
      network_id: N_switch_stacks_gathered
      state: merged
      config:
      - switch_stack_id: example
        name: Test-Config
        is_monitor_only: true
        virtual_mac: example
