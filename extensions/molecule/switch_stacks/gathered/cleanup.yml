---
- name: "Cleanup \u2014 switch_stacks (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_switch_stacks:
      network_id: N_switch_stacks_gathered
      state: deleted
      config:
      - switch_stack_id: example
    ignore_errors: true
