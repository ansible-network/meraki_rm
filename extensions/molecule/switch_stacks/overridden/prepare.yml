---
- name: "Prepare \u2014 switch_stacks (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_switch_stacks:
      network_id: N_switch_stacks_overridden
      state: merged
      config:
      - name: Test-Config
        is_monitor_only: true
        virtual_mac: example
      - name: Extra-To-Delete
        is_monitor_only: true
        virtual_mac: example
