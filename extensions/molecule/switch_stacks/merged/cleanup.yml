---
- name: "Cleanup \u2014 switch_stacks (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_switch_stacks:
      network_id: N_switch_stacks_merged
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
