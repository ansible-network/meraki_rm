---
- name: "Prepare — switch_link_aggregations (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_switch_link_aggregations:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
