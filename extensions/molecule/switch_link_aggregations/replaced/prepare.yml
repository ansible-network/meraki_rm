---
- name: "Prepare \u2014 switch_link_aggregations (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_switch_link_aggregations:
      network_id: N_switch_link_aggregations_replaced
      state: merged
      config:
      - link_aggregation_id: example
