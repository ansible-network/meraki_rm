---
- name: "Prepare \u2014 switch_link_aggregations (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_switch_link_aggregations:
      network_id: N_switch_link_aggregations_overridden
      state: merged
      config:
      - link_aggregation_id: example
      - link_aggregation_id: '999'
        name: Extra-To-Delete
