---
- name: "Cleanup \u2014 appliance_vlans (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_appliance_vlans:
      network_id: N_appliance_vlans_overridden
      state: deleted
      config:
      - vlan_id: '100'
    ignore_errors: true
