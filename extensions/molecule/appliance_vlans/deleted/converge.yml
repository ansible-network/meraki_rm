---
- name: "Converge \u2014 appliance_vlans (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete appliance_vlans configuration
    cisco.meraki_rm.meraki_appliance_vlans:
      network_id: N_123456789012345678
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
