---
- name: "Verify \u2014 wireless_air_marshal_rules (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Verify merged completed without error
    ansible.builtin.debug:
      msg: "Converge (merged) succeeded \u2014 no gathered state available for this resource"
