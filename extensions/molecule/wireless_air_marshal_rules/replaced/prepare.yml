---
- name: "Prepare \u2014 wireless_air_marshal_rules (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_wireless_air_marshal_rules:
      network_id: N_wireless_air_marshal_rules_replaced
      state: merged
      config:
      - type: access
        default_policy: example
        ssid: example
