---
- name: "Verify \u2014 wireless_air_marshal_rules (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Verify replaced completed without error
    ansible.builtin.debug:
      msg: "Converge (replaced) succeeded \u2014 no gathered state available for this resource"
