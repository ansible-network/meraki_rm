---
- name: "Cleanup \u2014 wireless_air_marshal_rules (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_wireless_air_marshal_rules:
      network_id: N_wireless_air_marshal_rules_overridden
      state: deleted
      config:
      - rule_id: example
    ignore_errors: true
