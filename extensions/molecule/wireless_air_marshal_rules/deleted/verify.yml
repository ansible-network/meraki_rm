---
- name: "Verify \u2014 wireless_air_marshal_rules (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Verify deleted completed without error
    ansible.builtin.debug:
      msg: "Converge (deleted) succeeded \u2014 no gathered state available for this resource"
