---
- name: "Prepare \u2014 wireless_air_marshal_rules (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_wireless_air_marshal_rules:
      network_id: N_wireless_air_marshal_rules_deleted
      state: merged
      config:
      - type: access
        default_policy: example
        ssid: example
