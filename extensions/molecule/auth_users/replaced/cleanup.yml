---
- name: "Cleanup \u2014 auth_users (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_auth_users:
      network_id: N_auth_users_replaced
      state: deleted
      config:
      - meraki_auth_users_id: example
    ignore_errors: true
