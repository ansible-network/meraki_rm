---
- name: "Prepare \u2014 auth_users (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_auth_users:
      network_id: N_auth_users_replaced
      state: merged
      config:
      - meraki_auth_user_id: example
        name: Test-Config
        email: admin@example.com
        password: example
        account_type: example
        is_admin: true
        email_password_to_user: true
