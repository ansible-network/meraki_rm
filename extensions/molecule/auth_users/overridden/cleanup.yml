---
- name: "Cleanup \u2014 auth_users (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_auth_users:
      network_id: N_auth_users_overridden
      state: deleted
      config:
      - meraki_auth_user_id: example
    ignore_errors: true
