---
- name: "Converge \u2014 auth_users (merged)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Create auth_users with merged state
    cisco.meraki_rm.meraki_auth_users:
      network_id: N_auth_users_merged
      state: merged
      config:
      - '{{ expected_config }}'
    register: merge_result
