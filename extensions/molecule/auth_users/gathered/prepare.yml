---
- name: "Prepare \u2014 auth_users (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_meraki_auth_users:
      network_id: N_123456789012345678
      state: merged
      config:
      - meraki_auth_users_id: example
        name: Test-Config
        email: admin@example.com
        password: example
        account_type: example
        is_admin: true
        email_password_to_user: true
