---
- name: "Cleanup \u2014 auth_users (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_auth_users:
      network_id: N_auth_users_gathered
      state: deleted
      config:
      - email: admin@example.com
    ignore_errors: true
