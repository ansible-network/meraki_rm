---
- name: "Cleanup \u2014 switch_settings (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: switch_settings does not support delete
