---
- name: "Prepare \u2014 switch_settings (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_switch_settings:
      network_id: N_switch_settings_check
      state: merged
      config:
      - '{{ prepare_config }}'
