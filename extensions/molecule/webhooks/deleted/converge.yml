---
- name: "Converge \u2014 webhooks (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete webhooks configuration
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_deleted
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
