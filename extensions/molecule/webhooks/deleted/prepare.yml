---
- name: "Prepare \u2014 webhooks (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_deleted
      state: merged
      config:
      - name: Test-Config
        url: https://example.com/webhook
        shared_secret: example
