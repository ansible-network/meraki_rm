---
- name: "Cleanup \u2014 webhooks (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_replaced
      state: deleted
      config:
      - http_server_id: example
    ignore_errors: true
