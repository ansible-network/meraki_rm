---
- name: "Prepare \u2014 webhooks (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_replaced
      state: merged
      config:
      - http_server_id: example
        name: Test-Config
        url: https://example.com/webhook
        shared_secret: example
