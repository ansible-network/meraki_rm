---
- name: "Converge \u2014 webhooks (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current webhooks configuration
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_gathered
      state: gathered
    register: gathered
