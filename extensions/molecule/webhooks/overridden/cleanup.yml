---
- name: "Cleanup \u2014 webhooks (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_webhooks:
      network_id: N_webhooks_overridden
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
