---
- name: "Prepare \u2014 switch_dhcp_policy (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_switch_dhcp_policy:
      network_id: N_switch_dhcp_policy_gathered
      state: merged
      config:
      - default_policy: example
