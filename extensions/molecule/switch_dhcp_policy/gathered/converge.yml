---
- name: "Converge \u2014 switch_dhcp_policy (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current switch_dhcp_policy configuration
    cisco.meraki_rm.meraki_switch_dhcp_policy:
      network_id: N_switch_dhcp_policy_gathered
      state: gathered
    register: gathered
