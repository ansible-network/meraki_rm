---
- name: "Cleanup \u2014 switch_dhcp_policy (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: switch_dhcp_policy does not support delete
