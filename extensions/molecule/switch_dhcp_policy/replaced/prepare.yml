---
- name: "Prepare \u2014 switch_dhcp_policy (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_switch_dhcp_policy:
      network_id: N_123456789012345678
      state: merged
      config:
      - default_policy: example
