---
- name: "Prepare \u2014 switch_dhcp_policy (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_switch_dhcp_policy:
      network_id: N_switch_dhcp_policy_check
      state: merged
      config:
      - '{{ prepare_config }}'
