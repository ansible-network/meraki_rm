---
- name: "Converge \u2014 appliance_security (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current appliance_security configuration
    cisco.meraki_rm.meraki_appliance_security:
      network_id: N_appliance_security_gathered
      state: gathered
    register: gathered
