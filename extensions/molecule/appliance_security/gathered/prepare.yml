---
- name: "Prepare \u2014 appliance_security (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_appliance_security:
      network_id: N_appliance_security_gathered
      state: merged
      config:
      - mode: none
        ids_rulesets: example
