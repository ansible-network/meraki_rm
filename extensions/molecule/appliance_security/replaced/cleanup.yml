---
- name: "Cleanup \u2014 appliance_security (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: appliance_security does not support delete
