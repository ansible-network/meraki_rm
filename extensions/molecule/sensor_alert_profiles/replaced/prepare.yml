---
- name: "Prepare \u2014 sensor_alert_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_sensor_alert_profiles:
      network_id: N_sensor_alert_profiles_replaced
      state: merged
      config:
      - name: Test-Config
        message: example
        include_sensor_url: true
