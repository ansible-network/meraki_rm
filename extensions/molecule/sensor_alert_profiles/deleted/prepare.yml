---
- name: "Prepare \u2014 sensor_alert_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_sensor_alert_profiles:
      network_id: N_123456789012345678
      state: merged
      config:
      - id: example
        name: Test-Config
        message: example
        include_sensor_url: true
