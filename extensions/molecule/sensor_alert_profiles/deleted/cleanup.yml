---
- name: "Cleanup \u2014 sensor_alert_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: "No-op \u2014 resource already deleted by converge"
    ansible.builtin.debug:
      msg: Nothing to clean up
