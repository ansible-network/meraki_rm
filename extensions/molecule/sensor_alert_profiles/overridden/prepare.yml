---
- name: "Prepare \u2014 sensor_alert_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_sensor_alert_profiles:
      network_id: N_sensor_alert_profiles_overridden
      state: merged
      config:
      - name: Test-Config
        message: example
        include_sensor_url: true
      - name: Extra-To-Delete
        message: example
        include_sensor_url: true
