---
- name: "Cleanup \u2014 sensor_alert_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_sensor_alert_profiles:
      network_id: N_sensor_alert_profiles_overridden
      state: deleted
      config:
      - id: example
    ignore_errors: true
