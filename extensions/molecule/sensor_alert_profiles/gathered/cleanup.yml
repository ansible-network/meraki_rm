---
- name: "Cleanup \u2014 sensor_alert_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_sensor_alert_profiles:
      network_id: N_sensor_alert_profiles_gathered
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
