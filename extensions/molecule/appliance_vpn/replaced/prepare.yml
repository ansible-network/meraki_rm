---
- name: "Prepare \u2014 appliance_vpn (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_appliance_vpn:
      network_id: N_appliance_vpn_replaced
      state: merged
      config:
      - mode: none
        subnet: 192.168.128.0/24
        enabled: true
        as_number: 1
        ibgp_hold_timer: 1
