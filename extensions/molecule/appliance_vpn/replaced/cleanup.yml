---
- name: "Cleanup \u2014 appliance_vpn (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: appliance_vpn does not support delete
