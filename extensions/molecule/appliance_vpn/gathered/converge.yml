---
- name: "Converge \u2014 appliance_vpn (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current appliance_vpn configuration
    cisco.meraki_rm.meraki_appliance_vpn:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
