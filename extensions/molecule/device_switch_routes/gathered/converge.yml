---
- name: "Converge \u2014 device_switch_routes (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current device_switch_routes configuration
    cisco.meraki_rm.meraki_device_switch_routes:
      serial: Q2XX-device_switch_routes-gathered
      state: gathered
    register: gathered
