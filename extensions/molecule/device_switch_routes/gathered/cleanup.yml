---
- name: "Cleanup \u2014 device_switch_routes (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_device_switch_routes:
      serial: Q2XX-XXXX-XXXX
      state: deleted
      config:
      - interface_id: example
        vlan_id: '100'
    ignore_errors: true
