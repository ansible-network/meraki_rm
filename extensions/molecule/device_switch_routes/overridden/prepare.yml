---
- name: "Prepare — device_switch_routes (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for overridden)
      cisco.meraki_rm.meraki_device_switch_routes:
        serial: Q2XX-XXXX-XXXX
        state: merged
        config:
          - "{{ expected_config }}"
