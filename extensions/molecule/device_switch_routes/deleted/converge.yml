---
- name: "Converge \u2014 device_switch_routes (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete device_switch_routes configuration
    cisco.meraki_rm.meraki_device_switch_routes:
      serial: Q2XX-device_switch_routes-deleted
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
