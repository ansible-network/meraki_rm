---
- name: "Prepare \u2014 organization_admins (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_organization_admins:
      organization_id: org_organization_admins_deleted
      state: merged
      config:
      - name: Test-Config
        email: admin@example.com
        org_access: full
        tags:
        - ansible
        - test
        authentication_method: Email
