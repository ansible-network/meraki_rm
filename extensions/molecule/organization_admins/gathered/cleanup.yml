---
- name: "Cleanup \u2014 organization_admins (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_admins:
      organization_id: '123456'
      state: deleted
      config:
      - admin_id: example
    ignore_errors: true
