---
- name: "Converge \u2014 organization_admins (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current organization_admins configuration
    cisco.meraki_rm.meraki_organization_admins:
      organization_id: '123456'
      state: gathered
    register: gathered
