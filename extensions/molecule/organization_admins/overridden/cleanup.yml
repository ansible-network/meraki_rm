---
- name: "Cleanup \u2014 organization_admins (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_admins:
      organization_id: org_organization_admins_overridden
      state: deleted
      config:
      - email: admin@example.com
    ignore_errors: true
