---
- name: "Prepare — organization_admins (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_organization_admins:
        organization_id: 'org_organization_admins_replaced'
        state: merged
        config:
          - "{{ expected_config }}"
