---
- name: "Converge \u2014 switch_acl (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Create switch_acl with merged state
    cisco.meraki_rm.meraki_switch_acl:
      network_id: N_123456789012345678
      state: merged
      config:
      - '{{ expected_config }}'
    register: merge_result
