---
- name: "Prepare — switch_acl (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Set ACL rules to baseline before test
      cisco.meraki_rm.meraki_switch_acl:
        network_id: N_switch_acl_merged
        state: replaced
        config:
          - rules:
              - comment: "Baseline rule"
                policy: allow
                ipVersion: ipv4
                protocol: any
                srcCidr: "any"
                srcPort: "any"
                dstCidr: "any"
                dstPort: "any"
                vlan: "1"
