---
- name: "Cleanup — switch_acl (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reset ACL rules to baseline after test
      cisco.meraki_rm.meraki_switch_acl:
        network_id: N_123456789012345678
        state: replaced
        config:
          - rules:
              - comment: "Baseline rule"
                policy: allow
                ipVersion: ipv4
                protocol: any
                srcCidr: "any"
                srcPort: "any"
                dstCidr: "any"
                dstPort: "any"
                vlan: "1"
