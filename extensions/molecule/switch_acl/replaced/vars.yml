---
expected_config:
  rules:
    - comment: "Allow HTTPS"
      policy: allow
      ipVersion: ipv4
      protocol: tcp
      srcCidr: "10.2.20.0/24"
      srcPort: "any"
      dstCidr: "0.0.0.0/0"
      dstPort: "443"
      vlan: "20"
