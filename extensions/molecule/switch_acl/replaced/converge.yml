---
- name: "Converge — switch_acl (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Replace switch_acl configuration
    cisco.meraki_rm.meraki_switch_acl:
      network_id: N_switch_acl_replaced
      state: replaced
      config:
      - '{{ expected_config }}'
    register: replace_result
