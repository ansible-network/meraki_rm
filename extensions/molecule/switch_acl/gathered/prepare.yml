---
- name: "Prepare — switch_acl (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_switch_acl:
        network_id: N_switch_acl_gathered
        state: merged
        config:
          - "{{ expected_config }}"
