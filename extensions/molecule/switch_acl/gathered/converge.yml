---
- name: "Converge \u2014 switch_acl (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current switch_acl configuration
    cisco.meraki_rm.meraki_switch_acl:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
