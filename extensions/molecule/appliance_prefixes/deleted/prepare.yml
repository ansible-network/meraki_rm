---
- name: "Prepare \u2014 appliance_prefixes (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_appliance_prefixes_deleted
      state: merged
      config:
      - static_delegated_prefix_id: example
        prefix: example
        description: Managed by Ansible
