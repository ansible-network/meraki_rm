---
- name: "Converge — appliance_prefixes (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current appliance_prefixes to get server-assigned IDs
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_123456789012345678
      state: gathered
    register: current

  - name: Delete all gathered appliance_prefixes
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_123456789012345678
      state: deleted
      config: "{{ current.gathered }}"
    register: delete_result
    when: current.gathered | length > 0
