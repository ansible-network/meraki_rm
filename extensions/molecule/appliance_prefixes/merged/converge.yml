---
- name: "Converge \u2014 appliance_prefixes (merged)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Create appliance_prefixes with merged state
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_appliance_prefixes_merged
      state: merged
      config:
      - '{{ expected_config }}'
    register: merge_result
