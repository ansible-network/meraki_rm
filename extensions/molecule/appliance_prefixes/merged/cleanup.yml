---
- name: "Cleanup \u2014 appliance_prefixes (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_appliance_prefixes_merged
      state: deleted
      config:
      - prefix: example
    ignore_errors: true
