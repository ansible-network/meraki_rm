---
- name: "Prepare \u2014 appliance_prefixes (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_appliance_prefixes_replaced
      state: merged
      config:
      - prefix: example
        description: Managed by Ansible
