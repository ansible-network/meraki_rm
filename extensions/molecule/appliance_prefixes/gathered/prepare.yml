---
- name: "Prepare \u2014 appliance_prefixes (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_appliance_prefixes:
      network_id: N_123456789012345678
      state: merged
      config:
      - static_delegated_prefix_id: example
        prefix: example
        description: Managed by Ansible
