---
- name: "Prepare \u2014 organization_adaptive_policy (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_organization_adaptive_policy:
      organization_id: '123456'
      state: merged
      config:
      - '{{ prepare_config }}'
