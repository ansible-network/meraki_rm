---
- name: "Cleanup \u2014 organization_adaptive_policy (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: organization_adaptive_policy does not support delete
