---
- name: "Prepare — organization_adaptive_policy (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_organization_adaptive_policy:
        organization_id: '123456'
        state: merged
        config:
          - "{{ expected_config }}"
