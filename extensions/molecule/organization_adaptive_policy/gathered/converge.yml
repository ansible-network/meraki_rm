---
- name: "Converge \u2014 organization_adaptive_policy (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current organization_adaptive_policy configuration
    cisco.meraki_rm.meraki_organization_adaptive_policy:
      organization_id: org_organization_adaptive_policy_gathered
      state: gathered
    register: gathered
