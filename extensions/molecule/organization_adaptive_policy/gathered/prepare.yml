---
- name: "Prepare \u2014 organization_adaptive_policy (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_organization_adaptive_policy:
      organization_id: '123456'
      state: merged
      config:
      - last_entry_rule: allow
