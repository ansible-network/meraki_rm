---
- name: "Cleanup \u2014 switch_access_policies (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_switch_access_policies:
      network_id: N_123456789012345678
      state: deleted
      config:
      - guest_vlan_id: 1
    ignore_errors: true
