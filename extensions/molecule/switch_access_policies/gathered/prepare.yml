---
- name: "Prepare \u2014 switch_access_policies (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_switch_access_policies:
      network_id: N_123456789012345678
      state: merged
      config:
      - access_policy_number: example
        name: Test-Config
        access_policy_type: example
        host_mode: example
        radius_accounting_enabled: true
        radius_coa_support_enabled: true
        guest_vlan_id: 1
