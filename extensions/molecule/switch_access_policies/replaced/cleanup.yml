---
- name: "Cleanup \u2014 switch_access_policies (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_switch_access_policies:
      network_id: N_switch_access_policies_replaced
      state: deleted
      config:
      - guest_vlan_id: 1
    ignore_errors: true
