---
- name: "Converge \u2014 switch_access_policies (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Replace switch_access_policies configuration
    cisco.meraki_rm.meraki_switch_access_policies:
      network_id: N_123456789012345678
      state: replaced
      config:
      - '{{ expected_config }}'
    register: replace_result
