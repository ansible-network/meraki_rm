---
- name: "Prepare \u2014 organization_config_templates (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_organization_config_templates:
      organization_id: org_organization_config_templates_overridden
      state: merged
      config:
      - name: Test-Config
        time_zone: example
        copy_from_network_id: example
      - name: Extra-To-Delete
        time_zone: example
        copy_from_network_id: example
