---
- name: "Prepare \u2014 organization_config_templates (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_organization_config_templates:
      organization_id: org_organization_config_templates_replaced
      state: merged
      config:
      - name: Test-Config
        time_zone: example
        copy_from_network_id: example
