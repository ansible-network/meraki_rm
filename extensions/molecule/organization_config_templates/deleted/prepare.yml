---
- name: "Prepare \u2014 organization_config_templates (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_organization_config_templates:
      organization_id: '123456'
      state: merged
      config:
      - config_template_id: example
        name: Test-Config
        time_zone: example
        copy_from_network_id: example
