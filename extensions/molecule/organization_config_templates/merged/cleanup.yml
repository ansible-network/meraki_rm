---
- name: "Cleanup \u2014 organization_config_templates (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_config_templates:
      organization_id: org_organization_config_templates_merged
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
