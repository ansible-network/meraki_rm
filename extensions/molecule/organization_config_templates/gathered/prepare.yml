---
- name: "Prepare \u2014 organization_config_templates (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_organization_config_templates:
      organization_id: org_organization_config_templates_gathered
      state: merged
      config:
      - config_template_id: example
        name: Test-Config
        time_zone: example
        copy_from_network_id: example
