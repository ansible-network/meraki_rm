---
- name: "Converge \u2014 switch_qos_rules (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current switch_qos_rules configuration
    cisco.meraki_rm.meraki_switch_qos_rules:
      network_id: N_switch_qos_rules_gathered
      state: gathered
    register: gathered
