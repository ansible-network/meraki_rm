---
- name: "Cleanup \u2014 switch_qos_rules (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_switch_qos_rules:
      network_id: N_switch_qos_rules_gathered
      state: deleted
      config:
      - dscp: 1
    ignore_errors: true
