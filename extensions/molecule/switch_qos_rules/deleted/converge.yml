---
- name: "Converge \u2014 switch_qos_rules (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete switch_qos_rules configuration
    cisco.meraki_rm.meraki_switch_qos_rules:
      network_id: N_switch_qos_rules_deleted
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
