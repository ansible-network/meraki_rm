---
- name: "Cleanup \u2014 camera_quality_retention_profiles (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_camera_quality_retention_profiles:
      network_id: N_camera_quality_retention_profiles_merged
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
