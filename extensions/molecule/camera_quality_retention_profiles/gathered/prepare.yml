---
- name: "Prepare — camera_quality_retention_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_camera_quality_retention_profiles:
        network_id: N_123456789012345678
        state: merged
        config:
          - "{{ expected_config }}"
