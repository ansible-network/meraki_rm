---
- name: "Cleanup \u2014 camera_quality_retention_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_camera_quality_retention_profiles:
      network_id: N_123456789012345678
      state: deleted
      config:
      - quality_retention_profile_id: example
        schedule_id: example
    ignore_errors: true
