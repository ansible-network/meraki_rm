---
- name: "Converge \u2014 camera_quality_retention_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current camera_quality_retention_profiles to get server-assigned IDs
    cisco.meraki_rm.meraki_camera_quality_retention_profiles:
      network_id: N_123456789012345678
      state: gathered
    register: current

  - name: Build config with actual server-assigned PK
    ansible.builtin.set_fact:
      replaced_config: >-
        {{ expected_config | combine({'quality_retention_profile_id': current.gathered[0].quality_retention_profile_id}) }}
    when: current.gathered | length > 0

  - name: Replaced camera_quality_retention_profiles configuration
    cisco.meraki_rm.meraki_camera_quality_retention_profiles:
      network_id: N_123456789012345678
      state: replaced
      config:
      - "{{ replaced_config }}"
    register: replaced_result
    when: replaced_config is defined
