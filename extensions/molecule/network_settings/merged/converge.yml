---
- name: "Converge \u2014 network_settings (merged)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Create network_settings with merged state
    cisco.meraki_rm.meraki_network_settings:
      network_id: N_network_settings_merged
      state: merged
      config:
      - '{{ expected_config }}'
    register: merge_result
