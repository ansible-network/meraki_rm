---
- name: "Cleanup \u2014 network_settings (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: network_settings does not support delete
