---
- name: "Prepare \u2014 network_settings (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_network_settings:
      network_id: N_network_settings_gathered
      state: merged
      config:
      - local_status_page_enabled: true
        remote_status_page_enabled: true
        reporting_enabled: true
        mode: none
