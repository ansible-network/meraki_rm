---
- name: "Cleanup \u2014 organization_saml (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: organization_saml does not support delete
