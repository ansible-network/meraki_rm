---
- name: "Converge \u2014 organization_saml (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current organization_saml configuration
    cisco.meraki_rm.meraki_organization_saml:
      organization_id: org_organization_saml_gathered
      state: gathered
    register: gathered
