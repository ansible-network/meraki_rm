---
- name: "Converge \u2014 organization_saml (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Replace organization_saml configuration
    cisco.meraki_rm.meraki_organization_saml:
      organization_id: '123456'
      state: replaced
      config:
      - '{{ expected_config }}'
    register: replace_result
