---
- name: "Converge \u2014 group_policies (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current group_policies configuration
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
