---
- name: "Prepare \u2014 group_policies (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_123456789012345678
      state: merged
      config:
      - group_policy_id: example
        name: Test-Config
        splash_auth_settings: example
