---
- name: "Prepare \u2014 group_policies (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_group_policies_replaced
      state: merged
      config:
      - group_policy_id: example
        name: Test-Config
        splash_auth_settings: example
