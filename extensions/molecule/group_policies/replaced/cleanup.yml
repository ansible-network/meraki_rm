---
- name: "Cleanup \u2014 group_policies (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_group_policies_replaced
      state: deleted
      config:
      - group_policy_id: example
    ignore_errors: true
