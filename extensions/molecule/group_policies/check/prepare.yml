---
- name: "Prepare \u2014 group_policies (check)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: "No-op \u2014 collection starts from empty state"
    ansible.builtin.debug:
      msg: Empty baseline for check mode test
