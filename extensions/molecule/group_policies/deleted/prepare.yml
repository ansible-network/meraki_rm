---
- name: "Prepare \u2014 group_policies (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_group_policies_deleted
      state: merged
      config:
      - group_policy_id: example
        name: Test-Config
        splash_auth_settings: example
