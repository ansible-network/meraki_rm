---
- name: "Prepare \u2014 group_policies (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_group_policies_overridden
      state: merged
      config:
      - group_policy_id: example
        name: Test-Config
        splash_auth_settings: example
      - group_policy_id: '999'
        name: Extra-To-Delete
        splash_auth_settings: example
