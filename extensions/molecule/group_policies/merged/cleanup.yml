---
- name: "Cleanup \u2014 group_policies (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_group_policies:
      network_id: N_group_policies_merged
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
