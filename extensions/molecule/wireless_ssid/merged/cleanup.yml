---
- name: "Cleanup \u2014 wireless_ssid (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: wireless_ssid does not support delete
