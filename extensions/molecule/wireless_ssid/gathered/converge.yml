---
- name: "Converge \u2014 wireless_ssid (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current wireless_ssid configuration
    cisco.meraki_rm.meraki_wireless_ssid:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
