---
- name: "Cleanup — wireless_ssid (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reset SSID 2 to baseline after test
      cisco.meraki_rm.meraki_wireless_ssid:
        network_id: N_123456789012345678
        state: replaced
        config:
          - number: 2
            name: "Unconfigured SSID 2"
            enabled: false
            auth_mode: open
            encryption_mode: wpa
            psk: ""
            wpa_encryption_mode: "WPA2 only"
            ip_assignment_mode: "NAT mode"
