---
- name: "Verify \u2014 wireless_rf_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current configuration
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_wireless_rf_profiles_gathered
      state: gathered
    register: gathered
  - name: Assert gathered configuration is not empty
    ansible.builtin.assert:
      that:
      - gathered.gathered is defined
      - gathered.gathered | length > 0
      fail_msg: "Gathered config is empty \u2014 expected at least one resource"
