---
- name: "Converge \u2014 wireless_rf_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Replace wireless_rf_profiles configuration
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_123456789012345678
      state: replaced
      config:
      - '{{ expected_config }}'
    register: replace_result
