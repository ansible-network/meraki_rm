---
- name: "Cleanup \u2014 wireless_rf_profiles (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather remaining wireless_rf_profiles resources
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_123456789012345678
      state: gathered
    register: current

  - name: Remove all test resources
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_123456789012345678
      state: deleted
      config: "{{ current.gathered }}"
    when: current.gathered | default([]) | length > 0
    ignore_errors: true
