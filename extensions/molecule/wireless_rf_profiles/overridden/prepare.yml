---
- name: "Prepare \u2014 wireless_rf_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for overridden)
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_123456789012345678
      state: merged
      config:
      - rf_profile_id: example
        name: Test-Config
        band_selection_type: example
        client_balancing_enabled: true
        is_indoor_default: true
        is_outdoor_default: true
      - rf_profile_id: '999'
        name: Extra-To-Delete
        band_selection_type: example
        client_balancing_enabled: true
        is_indoor_default: true
        is_outdoor_default: true
