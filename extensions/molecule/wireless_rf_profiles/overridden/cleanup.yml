---
- name: "Cleanup \u2014 wireless_rf_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_wireless_rf_profiles_overridden
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
