---
- name: "Prepare \u2014 wireless_rf_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_wireless_rf_profiles_deleted
      state: merged
      config:
      - name: Test-Config
        band_selection_type: ssid
        client_balancing_enabled: true
