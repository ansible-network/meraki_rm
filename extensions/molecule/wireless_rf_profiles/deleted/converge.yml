---
- name: "Converge \u2014 wireless_rf_profiles (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete wireless_rf_profiles configuration
    cisco.meraki_rm.meraki_wireless_rf_profiles:
      network_id: N_wireless_rf_profiles_deleted
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
