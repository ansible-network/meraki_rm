---
- name: "Prepare \u2014 switch_stp (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_switch_stp:
      network_id: N_123456789012345678
      state: merged
      config:
      - '{{ prepare_config }}'
