---
- name: "Prepare \u2014 switch_stp (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_switch_stp:
      network_id: N_switch_stp_gathered
      state: merged
      config:
      - rstp_enabled: true
