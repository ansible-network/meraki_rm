---
- name: "Cleanup \u2014 switch_stp (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: switch_stp does not support delete
