---
- name: "Prepare \u2014 switch_stp (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_switch_stp:
      network_id: N_switch_stp_replaced
      state: merged
      config:
      - rstp_enabled: true
