---
- name: "Prepare \u2014 organization_branding_policies (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_organization_branding_policies:
      organization_id: org_organization_branding_policies_deleted
      state: merged
      config:
      - name: Test-Config
        enabled: true
