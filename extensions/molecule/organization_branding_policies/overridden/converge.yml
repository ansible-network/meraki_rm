---
- name: "Converge \u2014 organization_branding_policies (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: "Override all organization_branding_policies \u2014 desired state only"
    cisco.meraki_rm.meraki_organization_branding_policies:
      organization_id: org_organization_branding_policies_overridden
      state: overridden
      config:
      - '{{ expected_config }}'
    register: override_result
