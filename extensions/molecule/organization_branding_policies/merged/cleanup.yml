---
- name: "Cleanup \u2014 organization_branding_policies (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_organization_branding_policies:
      organization_id: org_organization_branding_policies_merged
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
