---
- name: "Prepare — organization_branding_policies (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_organization_branding_policies:
        organization_id: 'org_organization_branding_policies_replaced'
        state: merged
        config:
          - "{{ expected_config }}"
