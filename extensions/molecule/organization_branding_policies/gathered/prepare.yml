---
- name: "Prepare \u2014 organization_branding_policies (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_organization_branding_policies:
      organization_id: org_organization_branding_policies_gathered
      state: merged
      config:
      - name: Test-Config
        enabled: true
