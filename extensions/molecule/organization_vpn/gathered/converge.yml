---
- name: "Converge \u2014 organization_vpn (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current organization_vpn configuration
    cisco.meraki_rm.meraki_organization_vpn:
      organization_id: '123456'
      state: gathered
    register: gathered
