---
- name: "Prepare — organization_vpn (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_organization_vpn:
        organization_id: '123456'
        state: merged
        config:
          - "{{ expected_config }}"
