---
- name: "Cleanup \u2014 organization_vpn (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: organization_vpn does not support delete
