---
- name: "Cleanup — organization_vpn (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Reset VPN config to baseline after test
      cisco.meraki_rm.meraki_organization_vpn:
        organization_id: "123456"
        state: replaced
        config:
          - peers:
              - name: "Baseline peer"
                publicIp: "203.0.113.1"
                privateSubnets:
                  - "10.255.0.0/24"
                secret: "baseline-secret"
