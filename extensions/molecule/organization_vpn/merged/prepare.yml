---
- name: "Prepare — organization_vpn (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Set VPN config to baseline before test
      cisco.meraki_rm.meraki_organization_vpn:
        organization_id: "123456"
        state: replaced
        config:
          - peers:
              - name: "Baseline peer"
                publicIp: "203.0.113.1"
                privateSubnets:
                  - "10.255.0.0/24"
                secret: "baseline-secret"
