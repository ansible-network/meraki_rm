---
- name: "Converge \u2014 organization_vpn (merged)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Create organization_vpn with merged state
    cisco.meraki_rm.meraki_organization_vpn:
      organization_id: '123456'
      state: merged
      config:
      - '{{ expected_config }}'
    register: merge_result
