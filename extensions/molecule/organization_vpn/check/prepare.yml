---
- name: "Prepare \u2014 organization_vpn (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_organization_vpn:
      organization_id: 'org_organization_vpn_check'
      state: merged
      config:
      - '{{ prepare_config }}'
