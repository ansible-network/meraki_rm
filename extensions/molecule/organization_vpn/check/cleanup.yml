---
- name: "Cleanup \u2014 organization_vpn (check)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup needed for singleton resource
    ansible.builtin.debug:
      msg: organization_vpn does not support delete
