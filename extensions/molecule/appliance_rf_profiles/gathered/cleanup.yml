---
- name: "Cleanup \u2014 appliance_rf_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_appliance_rf_profiles_gathered
      state: deleted
      config:
      - rf_profile_id: example
    ignore_errors: true
