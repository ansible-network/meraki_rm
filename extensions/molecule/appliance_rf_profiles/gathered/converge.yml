---
- name: "Converge \u2014 appliance_rf_profiles (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current appliance_rf_profiles configuration
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
