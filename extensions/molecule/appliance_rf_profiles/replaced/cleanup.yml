---
- name: "Cleanup \u2014 appliance_rf_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Remove test resources
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_appliance_rf_profiles_replaced
      state: deleted
      config:
      - name: Test-Config
    ignore_errors: true
