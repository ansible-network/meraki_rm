---
- name: "Prepare \u2014 appliance_rf_profiles (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_appliance_rf_profiles_replaced
      state: merged
      config:
      - rf_profile_id: example
        name: Test-Config
