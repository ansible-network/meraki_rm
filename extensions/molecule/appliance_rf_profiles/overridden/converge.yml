---
- name: "Converge \u2014 appliance_rf_profiles (overridden)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current appliance_rf_profiles to get server-assigned IDs
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_123456789012345678
      state: gathered
    register: current

  - name: Build config with actual server-assigned PK
    ansible.builtin.set_fact:
      overridden_config: >-
        {{ expected_config | combine({'rf_profile_id': current.gathered[0].rf_profile_id}) }}
    when: current.gathered | length > 0

  - name: Overridden appliance_rf_profiles configuration
    cisco.meraki_rm.meraki_appliance_rf_profiles:
      network_id: N_123456789012345678
      state: overridden
      config:
      - "{{ overridden_config }}"
    register: overridden_result
    when: overridden_config is defined
