---
- name: "Converge \u2014 device (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Gather current device configuration
    cisco.meraki_rm.meraki_device:
      serial: Q2XX-device-gathered
      state: gathered
    register: gathered
