---
- name: "Prepare — device (gathered)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for gathered)
      cisco.meraki_rm.meraki_device:
        serial: Q2XX-XXXX-XXXX
        state: merged
        config:
          - "{{ expected_config }}"
