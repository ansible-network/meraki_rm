---
- name: "Cleanup \u2014 device (check)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup needed for singleton resource
    ansible.builtin.debug:
      msg: device does not support delete
