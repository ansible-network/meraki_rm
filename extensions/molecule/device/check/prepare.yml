---
- name: "Prepare \u2014 device (check)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Seed singleton baseline via merged
    cisco.meraki_rm.meraki_device:
      serial: Q2XX-XXXX-XXXX
      state: merged
      config:
      - '{{ prepare_config }}'
