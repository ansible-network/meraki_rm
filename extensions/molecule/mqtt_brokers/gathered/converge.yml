---
- name: "Converge \u2014 mqtt_brokers (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current mqtt_brokers configuration
    cisco.meraki_rm.meraki_mqtt_brokers:
      network_id: N_123456789012345678
      state: gathered
    register: gathered
