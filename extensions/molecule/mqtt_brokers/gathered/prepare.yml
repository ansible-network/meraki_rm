---
- name: "Prepare \u2014 mqtt_brokers (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for gathered)
    cisco.meraki_rm.meraki_mqtt_brokers:
      network_id: N_mqtt_brokers_gathered
      state: merged
      config:
      - name: Test-Config
        host: mqtt.example.com
        port: 1883
