---
- name: "Prepare \u2014 mqtt_brokers (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for replaced)
    cisco.meraki_rm.meraki_mqtt_brokers:
      network_id: N_mqtt_brokers_replaced
      state: merged
      config:
      - name: Test-Config
        host: mqtt.example.com
        port: 1883
