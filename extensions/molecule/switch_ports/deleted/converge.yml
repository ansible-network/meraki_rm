---
- name: "Converge \u2014 switch_ports (deleted)"
  hosts: localhost
  gather_facts: false
  vars_files:
  - vars.yml
  tasks:
  - name: Delete switch_ports configuration
    cisco.meraki_rm.meraki_switch_ports:
      serial: Q2XX-XXXX-XXXX
      state: deleted
      config:
      - '{{ expected_config }}'
    register: delete_result
