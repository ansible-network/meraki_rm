---
- name: "Prepare \u2014 switch_ports (deleted)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Seed resource via merged (prerequisite for deleted)
    cisco.meraki_rm.meraki_switch_ports:
      serial: Q2XX-XXXX-XXXX
      state: merged
      config:
      - port_id: '1'
        name: Test-Config
        tags:
        - ansible
        - test
        enabled: true
        type: access
        vlan: 1
        voice_vlan: 100
        allowed_vlans: all
