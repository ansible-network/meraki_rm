---
- name: "Cleanup \u2014 switch_ports (replaced)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: No cleanup configured for singleton resource
    ansible.builtin.debug:
      msg: switch_ports does not support delete
