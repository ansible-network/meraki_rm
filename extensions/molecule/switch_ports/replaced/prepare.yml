---
- name: "Prepare — switch_ports (replaced)"
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../merged/vars.yml
  tasks:
    - name: Seed resource via merged (prerequisite for replaced)
      cisco.meraki_rm.meraki_switch_ports:
        serial: Q2XX-XXXX-XXXX
        state: merged
        config:
          - "{{ expected_config }}"
