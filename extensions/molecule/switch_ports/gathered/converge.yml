---
- name: "Converge \u2014 switch_ports (gathered)"
  hosts: localhost
  gather_facts: false
  tasks:
  - name: Gather current switch_ports configuration
    cisco.meraki_rm.meraki_switch_ports:
      serial: Q2XX-XXXX-XXXX
      state: gathered
    register: gathered
